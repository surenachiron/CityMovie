"use client"
import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        redmain: "#f44336",
        white: "#ffffff",
        black: "#000",
        mode: 'dark',
    },
});

export default theme